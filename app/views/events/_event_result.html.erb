<tbody>
  <tr id="<%= event_result.id %>" data-href="<%= competitor_path(event_result.competitor) %>">
    <td><%= event_result.place %></td>
    <td><%= event_result.competitor.full_name %></td>
    <td><%= event_result.points %></td>
  </tr>
</tbody>